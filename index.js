// For...of

const fakeData = {name: "john", age: 90, lastName: "doe"}

const fakeArr = ["john", 'doe', 78]

// Arrays, Set, NodeList 

// fakeArr.map()
// fakeArr.forEach()

for (const value of fakeArr) {
    console.log(value)
}
